import { fiscalScenarios, type FiscalScenario } from './fiscalPolicyScenarios';
import styles from './fiscalPolicy.module.css';
function Answer({scenario:s}:{scenario:FiscalScenario}){return <><p><b>完整解释：</b>{s.explanation}</p>{s.choices.map((c,i)=><p key={c.id}><b>{String.fromCharCode(65+i)}路径：</b>{c.diagnosis}</p>)}<p className="section-sources"><b>依据：</b>{s.sourceIds.map(id=><a href={`#ref-${id}`} key={id} aria-label={`参考文献${id}`}>[{id}]</a>)}</p></>;}
export default function FiscalStaticQuiz(){return <>{fiscalScenarios.map((s,i)=><article key={s.id} className={styles.staticQuiz} data-fiscal-static={`K${i+1}`}><h3>K{i+1} · {s.title}</h3><p><b>题设：</b>{s.input}</p><p>{s.question}</p><ol className={styles.choiceList}>{s.choices.map((c,index)=><li key={c.id} data-fiscal-choice={String.fromCharCode(65+index)}><b>{String.fromCharCode(65+index)} · </b>{c.label}</li>)}</ol><details><summary>原生展开：正确推理及全部三条路径诊断</summary><Answer scenario={s}/></details><div className={styles.printAnswer}><Answer scenario={s}/></div></article>)}</>;}
